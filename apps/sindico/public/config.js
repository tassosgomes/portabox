// Dev/fallback stub. In production (Cloudflare Pages), this path is served by
// functions/config.js.ts, which takes precedence over this static asset and
// injects runtime env from the Pages project environment variables.
window.__ENV__ = window.__ENV__ || {};
